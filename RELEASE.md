# 🚀 DuckLoop v2.1.0 Release

## 📦 发布信息

| 项目 | 详情 |
|------|------|
| **版本号** | v2.1.0 |
| **发布日期** | 2024年 |
| **版本类型** | 正式版 |
| **APK 文件** | `app/release/DuckLoop_v2.1.0.apk` |

---

## ✨ 新功能

### 🗑️ 文件管理功能
- 添加「清空」按钮，支持一键清空背景音乐列表
- 添加「清空」按钮，支持一键清空播报音频列表
- 删除前显示确认对话框，防止误删
- 录音文件也会被一并清理

---

## 🐛 Bug 修复

### 🔧 GlobalScope 内存泄漏修复
- **问题**: `AnnouncePlayer.kt` 中使用了 `GlobalScope.launch`，可能导致内存泄漏
- **方案**: 改为使用 `withTimeoutOrNull` 和结构化并发
- **影响**: 提升应用稳定性，减少内存占用

### 🔧 播报循环问题修复
- **问题**: 多文件播报时，播放完最后一个文件后不会自动回到第一个循环播放
- **方案**: 在 `LoopService.kt` 中添加循环播放逻辑，支持顺序播放所有播报文件
- **影响**: 播报功能现在可以正确循环播放所有音频文件

---

## 📝 更新内容

### 项目文档
- 更新 `README.md`，添加新功能说明
- 更新 `CHANGELOG.md`，记录版本变更
- 更新 `TODO.md`，标记已完成任务
- 新增 `项目结构说明.md`，提供详细的项目结构文档

### 代码优化
- 移除不必要的 `GlobalScope` 使用
- 改进音频播放的稳定性
- 优化内存管理

---

## 📊 性能改进

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| 内存占用 | 较高 (GlobalScope) | 优化 (结构化并发) |
| 播报循环 | 不支持多文件循环 | 支持多文件循环播放 |
| 文件管理 | 不支持删除 | 支持清空列表 |

---

## 🔧 技术细节

### 修改的文件

1. **AnnouncePlayer.kt**
   - 移除 `GlobalScope` 使用
   - 添加 `withTimeoutOrNull` 超时机制
   - 添加 `seekTo()` 方法支持多文件播放

2. **LoopService.kt**
   - 修改播报循环逻辑
   - 支持顺序播放所有播报文件
   - 添加播放状态跟踪

3. **MainActivity.kt**
   - 添加清空背景音乐功能
   - 添加清空播报音频功能
   - 添加确认对话框

4. **activity_main.xml**
   - 添加清空背景音乐按钮
   - 添加清空播报音频按钮

---

## 📦 安装说明

### 系统要求
- Android 8.0 (API 26) 或更高版本
- 存储空间: 约 10 MB

### 安装步骤
1. 下载 `DuckLoop_v2.1.0.apk`
2. 在设备上启用「未知来源」安装
3. 点击 APK 文件进行安装
4. 授予必要的权限（录音、通知等）

---

## 🔄 升级说明

### 从 v2.0.0 升级
- 直接安装新版本 APK 即可
- 用户数据和设置会保留
- 建议重启应用以确保所有更改生效

---

## 🐛 已知问题

1. **音频焦点冲突**
   - 与其他音乐应用同时播放时可能产生冲突
   - 计划在后续版本优化

2. **录音文件覆盖**
   - 每次录音会覆盖上一次的录音文件
   - 计划添加录音历史管理

---

## 📝 下一步计划

### v2.2.0 计划功能
- 支持播放列表拖拽排序
- 添加播报音频淡入淡出效果
- 完善深色模式支持
- 添加单元测试

---

## 📞 反馈与支持

如有任何问题或建议，请通过以下方式联系：

- **GitHub Issues**: [项目 Issues 页面]
- **邮箱**: [请补充]

---

## 📄 许可证

本项目为私人项目，版权所有。

---

**发布者**: Eason
**发布日期**: 2024年
